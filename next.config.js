/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost', 'xsgames.co', 'dhddcpuojgrszssuuisa.supabase.co'],
  },
};

module.exports = nextConfig; 